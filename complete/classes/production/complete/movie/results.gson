import groovy.transform.Field
import neo4j.movies.Movie

@Field Collection<Movie> movieList = []

json(movieList) { Movie movie ->
    released movie.released
    tagline movie.tagline
    title movie.title
}